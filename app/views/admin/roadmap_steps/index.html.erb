<% content_for :page_title, "Étapes - #{@roadmap.title}" %>

<div class="mb-4">
  <%= link_to "← Retour à la roadmap", admin_field_roadmap_path(@roadmap.field, @roadmap), class: "text-blue-600 hover:underline" %>
</div>

<div class="mb-4 flex items-center justify-between">
  <div>
    <h1 class="text-xl font-semibold">Étapes de la roadmap</h1>
    <p class="text-gray-600"><%= @roadmap.title %> - <%= @roadmap.field.name %></p>
  </div>
  <%= link_to "Nouvelle étape", new_admin_field_roadmap_roadmap_step_path(@roadmap.field, @roadmap), class: "inline-flex items-center bg-primary text-white px-4 py-2 rounded hover:bg-primary-600" %>
</div>

<% if @roadmap_steps.any? %>
  <div class="space-y-4">
    <% @roadmap_steps.each do |step| %>
      <div class="bg-white rounded-lg shadow p-6">
        <div class="flex items-start justify-between">
          <div class="flex items-start gap-4 flex-1">
            <span class="flex items-center justify-center w-10 h-10 bg-primary text-white font-bold rounded-full">
              <%= step.order %>
            </span>
            
            <div class="flex-1">
              <h3 class="text-lg font-medium mb-2"><%= step.title %></h3>
              
              <div class="space-y-3 text-sm">
                <div>
                  <span class="font-medium text-orange-600">🎯 Objectif :</span>
                  <p class="mt-1 text-gray-700"><%= simple_format(step.objective) %></p>
                </div>
                
                <% if step.skills.present? %>
                  <div>
                    <span class="font-medium text-green-600">✅ Compétences :</span>
                    <p class="mt-1 text-gray-700"><%= simple_format(step.skills) %></p>
                  </div>
                <% end %>
                
                <% if step.activities.present? %>
                  <div>
                    <span class="font-medium text-blue-600">📌 Activités :</span>
                    <p class="mt-1 text-gray-700"><%= simple_format(step.activities) %></p>
                  </div>
                <% end %>
              </div>
            </div>
          </div>
          
          <div class="flex flex-col gap-2 ml-4">
            <%= link_to "Voir", admin_field_roadmap_roadmap_step_path(@roadmap.field, @roadmap, step), class: "text-blue-600 hover:underline text-sm" %>
            <%= link_to "Modifier", edit_admin_field_roadmap_roadmap_step_path(@roadmap.field, @roadmap, step), class: "text-green-600 hover:underline text-sm" %>
            <%= button_to "Supprimer", admin_field_roadmap_roadmap_step_path(@roadmap.field, @roadmap, step), method: :delete, 
                form: { data: { turbo_confirm: "Êtes-vous sûr ?" } }, 
                class: "text-red-600 hover:underline text-sm" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
  
  <div class="mt-6">
    <%= pagy_nav(@pagy) if respond_to?(:pagy_nav) %>
  </div>
<% else %>
  <div class="text-center py-12 bg-gray-50 rounded-lg">
    <div class="text-gray-400 text-4xl mb-4">📋</div>
    <h3 class="text-lg font-medium text-gray-900 mb-2">Aucune étape</h3>
    <p class="text-gray-600 mb-4">Commencez par créer la première étape de cette roadmap.</p>
    <%= link_to "Créer une étape", new_admin_field_roadmap_roadmap_step_path(@roadmap.field, @roadmap), class: "inline-flex items-center bg-primary text-white px-4 py-2 rounded hover:bg-primary-600" %>
  </div>
<% end %>