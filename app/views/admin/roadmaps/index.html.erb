<% content_for :page_title, "Roadmaps - #{@field.name}" %>

<div class="mb-4">
  <%= link_to "← Retour aux filières", admin_fields_path, class: "text-blue-600 hover:underline" %>
</div>

<div class="mb-4 flex items-center justify-between">
  <div>
    <h1 class="text-xl font-semibold">Roadmaps</h1>
    <p class="text-gray-600">Filière : <%= @field.name %></p>
  </div>
  <%= link_to "Nouvelle roadmap", new_admin_field_roadmap_path(@field), class: "inline-flex items-center bg-primary text-white px-4 py-2 rounded hover:bg-primary-600" %>
</div>

<% if @roadmaps.any? %>
  <%= render "shared/table",
    columns: [
      { header: "Titre",  cell: ->(r){ link_to r.title, admin_field_roadmap_path(@field, r), class: "text-blue-600 hover:underline font-medium" } },
      { header: "Description", cell: ->(r){ truncate(r.description.to_s, length: 100) } },
      { header: "Étapes", cell: ->(r){ r.roadmap_steps.count } },
      { header: "Créée le", cell: ->(r){ l(r.created_at, format: :short) } }
    ],
    rows: @roadmaps,
    pagy: @pagy,
    actions: ->(r){ link_to("Voir", admin_field_roadmap_path(@field, r), class: "text-blue-600 hover:underline") +
                    " | ".html_safe +
                    link_to("Modifier", edit_admin_field_roadmap_path(@field, r), class: "text-green-600 hover:underline") +
                    " | ".html_safe +
                    link_to("Étapes", admin_field_roadmap_roadmap_steps_path(@field, r), class: "text-purple-600 hover:underline") +
                    " | ".html_safe +
                    button_to("Supprimer", admin_field_roadmap_path(@field, r), method: :delete, form: { data: { turbo_confirm: "Êtes-vous sûr ?" } }, class: "text-red-600 hover:underline inline") } %>
<% else %>
  <div class="text-center py-12 bg-gray-50 rounded-lg">
    <div class="text-gray-400 text-4xl mb-4">📚</div>
    <h3 class="text-lg font-medium text-gray-900 mb-2">Aucune roadmap</h3>
    <p class="text-gray-600 mb-4">Commencez par créer votre première roadmap pour cette filière.</p>
    <%= link_to "Créer une roadmap", new_admin_field_roadmap_path(@field), class: "inline-flex items-center bg-primary text-white px-4 py-2 rounded hover:bg-primary-600" %>
  </div>
<% end %>