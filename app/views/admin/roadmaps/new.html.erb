<% content_for :page_title, "Nouvelle roadmap" %>

<div class="mb-4">
  <%= link_to "← Retour", admin_field_roadmaps_path(@field), class: "text-blue-600 hover:underline" %>
</div>

<div class="bg-white rounded shadow p-6">
  <h1 class="text-xl font-semibold mb-4">Nouvelle roadmap pour <%= @field.name %></h1>
  <%= render "form" %>
</div>