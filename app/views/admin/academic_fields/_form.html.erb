<div class="glass-card rounded-2xl p-8 shadow-premium animate-premium-in max-w-2xl">
  <%= form_with model: [:admin, academic_field], class: "space-y-6" do |form| %>
    <% if academic_field.errors.any? %>
      <div class="bg-red-50 border border-red-100 p-4 rounded-xl text-sm text-red-700 mb-6 animate-shake">
        <div class="font-bold flex items-center gap-2 mb-1">
          <%= lucide_icon :alert_circle, class: "w-4 h-4" %>
          <%= t("errors.messages.not_saved", count: academic_field.errors.count, resource: "academic field") %>
        </div>
        <ul class="list-disc list-inside opacity-90">
          <% academic_field.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="space-y-1">
      <%= form.label :name, class: "text-[10px] font-black uppercase tracking-widest text-slate-400" %>
      <%= form.text_field :name, placeholder: "Ex: Langues",
            class: "w-full bg-slate-50 border-0 rounded-xl px-4 py-3 text-sm font-medium focus:ring-2 focus:ring-[var(--color-primary)]/20 transition-all" %>
    </div>

    <div class="space-y-1">
      <%= form.label :slug, class: "text-[10px] font-black uppercase tracking-widest text-slate-400" %>
      <%= form.text_field :slug, placeholder: "Ex: langues",
            class: "w-full bg-slate-50 border-0 rounded-xl px-4 py-3 text-sm font-medium focus:ring-2 focus:ring-[var(--color-primary)]/20 transition-all" %>
    </div>

    <div class="space-y-1">
      <%= form.label :position, "Position (ordre d'affichage)", class: "text-[10px] font-black uppercase tracking-widest text-slate-400" %>
      <%= form.number_field :position, min: 1,
            class: "w-full bg-slate-50 border-0 rounded-xl px-4 py-3 text-sm font-medium focus:ring-2 focus:ring-[var(--color-primary)]/20 transition-all" %>
    </div>

    <div class="pt-4 flex items-center gap-4">
      <%= form.submit class: "bg-[var(--color-primary)] text-white px-8 py-3 rounded-xl font-bold text-sm uppercase tracking-widest hover:opacity-90 transition-all shadow-sm transform hover:translate-y-[-1px] cursor-pointer" %>
      <%= link_to t("Cancel"), admin_academic_fields_path, class: "text-slate-400 text-xs font-bold uppercase tracking-widest hover:text-slate-600 transition-colors" %>
    </div>
  <% end %>
</div>
