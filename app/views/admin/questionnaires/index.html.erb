<% content_for :page_title, "Questionnaires" %>

<%= render "shared/page_header", 
    title: "Questionnaires",
    subtitle: "Gérer les différents modèles de diagnostic",
    action_text: "Nouveau Questionnaire",
    action_url: new_admin_questionnaire_path,
    action_icon: "plus" %>

<%= render "shared/table",
  columns: [
    { header: "Titre", cell: ->(q){ content_tag(:div, q.title, class: "font-bold text-slate-800") } },
    { header: "Description", cell: ->(q){ content_tag(:div, q.description&.truncate(60), class: "text-slate-500 text-sm") } },
    { header: "Questions", cell: ->(q){ content_tag(:span, q.questions.count, class: "font-mono font-bold text-xs bg-slate-100 px-2 py-0.5 rounded text-slate-600") } },
    { header: "Blocs", cell: ->(q){ content_tag(:span, q.total_blocs, class: "font-mono font-bold text-xs bg-slate-100 px-2 py-0.5 rounded text-slate-600") } },
    { header: "Statut", cell: ->(q){ render "shared/badge", text: (q.active? ? "Actif" : "Inactif"), color: (q.active? ? :green : :gray) } }
  ],
  rows: @questionnaires,
  pagy: @pagy,
  actions: ->(q){
    capture do %>
      <% unless q.active? %>
        <%= button_to activate_admin_questionnaire_path(q), method: :patch, class: "mr-2 p-2 bg-green-50 text-green-600 rounded-lg hover:bg-green-100 transition-colors border-0", title: "Activer" do %>
          <%= lucide_icon "check-circle", class: "w-4 h-4" %>
        <% end %>
      <% end %>
      
      <%= link_to admin_questionnaire_questions_path(q), class: "mr-2 p-2 bg-blue-50 text-blue-600 rounded-lg hover:bg-blue-100 transition-colors", title: "Voir les questions" do %>
        <%= lucide_icon "list", class: "w-4 h-4" %>
      <% end %>

      <%= link_to edit_admin_questionnaire_path(q), class: "mr-2 p-2 bg-slate-100 text-slate-700 rounded-lg hover:bg-slate-200 transition-colors" do %>
        <%= lucide_icon "edit-3", class: "w-4 h-4" %>
      <% end %>
      
      <%= button_to admin_questionnaire_path(q), method: :delete, data: { turbo_confirm: "Confirmer la suppression ?" }, class: "p-2 bg-red-50 text-red-600 rounded-lg hover:bg-red-100 transition-colors border-0" do %>
        <%= lucide_icon "trash-2", class: "w-4 h-4" %>
      <% end %>
    <% end
  } %>
