<%# app/views/admin/trajectories/_form.html.erb %>
<%= form_with model: [:admin, trajectory], class: "space-y-4 max-w-2xl" do |f| %>
  <% if trajectory.errors.any? %>
    <div class="bg-red-50 border border-red-200 p-3 rounded text-sm text-red-700">
      <% trajectory.errors.full_messages.each do |msg| %><div><%= msg %></div><% end %>
    </div>
  <% end %>

  <div>
    <%= f.label :profile_id, "Profil", class: "block text-sm font-medium text-gray-700 mb-1" %>
    <%= f.collection_select :profile_id, @profiles, :id, :name,
          { prompt: "Choisir un profil" }, class: "w-full border rounded px-3 py-2 text-sm" %>
  </div>
  <div>
    <%= f.label :axe_1, "Axe 1 — Institutionnel / ONG", class: "block text-sm font-medium text-gray-700 mb-1" %>
    <%= f.text_area :axe_1, rows: 3, class: "w-full border rounded px-3 py-2 text-sm" %>
  </div>
  <div>
    <%= f.label :axe_2, "Axe 2 — Secteur privé / hybride", class: "block text-sm font-medium text-gray-700 mb-1" %>
    <%= f.text_area :axe_2, rows: 3, class: "w-full border rounded px-3 py-2 text-sm" %>
  </div>
  <div>
    <%= f.label :axe_3, "Axe 3 — Spécialisation long terme", class: "block text-sm font-medium text-gray-700 mb-1" %>
    <%= f.text_area :axe_3, rows: 3, class: "w-full border rounded px-3 py-2 text-sm" %>
  </div>
  <div class="flex items-center gap-2">
    <%= f.check_box :active, class: "accent-blue-600" %>
    <%= f.label :active, "Active", class: "text-sm font-medium text-gray-700" %>
  </div>

  <%= f.submit "Enregistrer", class: "bg-blue-700 text-white px-6 py-2 rounded font-medium text-sm" %>
<% end %>
