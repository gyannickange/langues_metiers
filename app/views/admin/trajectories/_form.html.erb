<div class="glass-card rounded-2xl p-8 shadow-premium animate-premium-in max-w-2xl">
  <%= form_with model: [:admin, trajectory], class: "space-y-6" do |f| %>
    <% if trajectory.errors.any? %>
      <div class="bg-red-50 border border-red-100 p-4 rounded-xl text-sm text-red-700 mb-6 animate-shake">
        <div class="font-bold flex items-center gap-2 mb-1">
          <%= lucide_icon "alert-circle", class: "w-4 h-4" %>
          <%= t("errors.messages.not_saved", count: trajectory.errors.count, resource: "trajectoire") %>
        </div>
        <ul class="list-disc list-inside opacity-90">
          <% trajectory.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <%= render partial: "shared/form_field", locals: { form: f, field: :career_id, label: "Métier associé", type: "select",
          options: @careers.map { |c| [c.title, c.id] }, prompt: "Choisir un métier..." } %>

    <%= render partial: "shared/form_field", locals: { form: f, field: :axe_1, label: "Axe 1 — Institutionnel / ONG", type: "text_area", rows: 3, placeholder: "Entrez le contenu de l'axe 1..." } %>

    <%= render partial: "shared/form_field", locals: { form: f, field: :axe_2, label: "Axe 2 — Secteur privé / hybride", type: "text_area", rows: 3, placeholder: "Entrez le contenu de l'axe 2..." } %>

    <%= render partial: "shared/form_field", locals: { form: f, field: :axe_3, label: "Axe 3 — Spécialisation long terme", type: "text_area", rows: 3, placeholder: "Entrez le contenu de l'axe 3..." } %>

    <%= render partial: "shared/form_field", locals: { form: f, field: :active, label: "Trajectoire Active", type: "checkbox" } %>

    <div class="pt-4 flex items-center gap-4">
      <%= f.submit "Enregistrer", class: "bg-[var(--color-primary)] text-white px-8 py-3 rounded-xl font-bold text-sm uppercase hover:opacity-90 transition-all shadow-sm transform hover:translate-y-[-1px]" %>
      <%= link_to "Annuler", admin_trajectories_path, class: "text-slate-400 text-xs font-bold uppercase hover:text-slate-600 transition-colors" %>
    </div>
  <% end %>
</div>
