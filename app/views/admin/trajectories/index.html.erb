<% content_for :page_title, "Trajectoires" %>

<%= render "shared/page_header", 
    title: "Trajectoires",
    subtitle: "Gérer les trajectoires professionnelles et profils associés",
    action_text: "Nouvelle Trajectoire",
    action_url: new_admin_trajectory_path,
    action_icon: "plus" %>

<%= render "shared/table",
    columns: [
      { header: "Profil", cell: ->(t){ t.profile&.name } },
      { header: "Axe Principal", cell: ->(t){ content_tag(:span, t.axe_1&.truncate(60), class: "text-slate-500") } },
      { header: "Statut", cell: ->(t){ render "shared/badge", text: (t.active? ? "Active" : "Inactive"), color: (t.active? ? :green : :gray) } }
    ],
    rows: @trajectories,
    pagy: @pagy,
    actions: ->(t){
      capture do %>
        <%= link_to edit_admin_trajectory_path(t), class: "p-2 bg-slate-100 text-slate-700 rounded-lg hover:bg-slate-200 transition-colors" do %>
          <%= lucide_icon "edit-3", class: "w-4 h-4" %>
        <% end %>
        <%= button_to admin_trajectory_path(t), method: :delete, data: { turbo_confirm: "Confirmer la suppression ?" }, class: "p-2 bg-red-50 text-red-600 rounded-lg hover:bg-red-100 transition-colors border-0" do %>
          <%= lucide_icon "trash-2", class: "w-4 h-4" %>
        <% end %>
      <% end
    } %>
