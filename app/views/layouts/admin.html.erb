<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Admin" %> · Insertrix</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="bg-slate-50/50 antialiased font-sans overflow-hidden">
    <%= render "shared/flash" %>

    <div data-controller="menu" class="h-screen flex flex-col md:flex-row p-0 md:p-4 lg:p-6 gap-0 md:gap-4 lg:gap-6">
      <!-- Sidebar / Navigation -->
      <aside data-menu-target="sidebar" 
             class="w-72 bg-white/80 backdrop-blur-xl border border-slate-200/50 rounded-none md:rounded-[2.5rem] shadow-premium hidden md:flex flex-col fixed inset-y-0 left-0 md:relative z-40 transform -translate-x-full md:translate-x-0 transition-all duration-500 ease-in-out">
        
        <div class="p-8 flex items-center justify-between">
          <div class="flex items-center gap-4">
            <div class="group flex items-center justify-center space-x-2 text-secondary">
              <span class="text-3xl font-extrabold tracking-tight">
                Insertrix<span class="text-primary">.</span>
              </span>
            </div>
          </div>
          <button data-action="menu#toggle" class="p-2 md:hidden text-slate-400 hover:text-slate-600 transition-colors">
            <%= lucide_icon "x", class: "w-6 h-6" %>
          </button>
        </div>

        <div class="flex-1 px-4 overflow-y-auto custom-scrollbar">
          <%= render "shared/sidebar" %>
        </div>

        <div class="p-6 mt-auto">
          <div class="bg-slate-50 rounded-3xl p-4 flex items-center justify-between border border-slate-100/50">
            <div class="flex items-center gap-3">
              <div class="w-8 h-8 rounded-full bg-slate-200 flex items-center justify-center overflow-hidden">
                <%= lucide_icon "user", class: "w-4 h-4 text-slate-400" %>
              </div>
              <div class="flex flex-col">
                <span class="text-[10px] font-black uppercase text-slate-400">Admin Mode</span>
                <span class="text-xs font-bold text-slate-700 truncate w-24"><%= current_user&.email&.split('@')&.first %></span>
              </div>
            </div>
            <div class="flex items-center">
              <div class="w-2 h-2 rounded-full bg-green-500 animate-pulse"></div>
            </div>
          </div>
        </div>
      </aside>

      <!-- Main Content -->
      <div class="flex-1 flex flex-col min-w-0 min-h-0 bg-white/90 backdrop-blur-sm rounded-none md:rounded-[3rem] border border-slate-200/30 shadow-premium-lg overflow-hidden relative">
        <header class="h-20 flex items-center px-8 border-b border-slate-100/50 sticky top-0 z-30 bg-white/40 backdrop-blur-md md:hidden">
          <button class="p-3 text-slate-600 hover:bg-slate-100 rounded-2xl transition-all" data-action="menu#toggle" aria-label="Open menu">
            <%= lucide_icon "menu", class: "w-6 h-6" %>
          </button>
        </header>

        <main class="flex-1 min-h-0 p-6 md:p-8 lg:p-10 overflow-y-auto custom-scrollbar">
          <%= yield %>
        </main>
      </div>
    </div>
  </body>
  </html>


