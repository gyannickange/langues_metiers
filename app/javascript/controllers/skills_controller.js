import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  static targets = ["level"]

  select(event) {
    // Optionally react to skill change; placeholder for dynamic behaviors
  }
}


